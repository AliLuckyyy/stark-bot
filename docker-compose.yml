services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      # Persistent database storage
      - ./data:/app/.db
    restart: unless-stopped
